import { Meteor } from 'meteor/meteor';

import '../imports/MessageQueue/server/methods'
import '../imports/MessageQueue/server/publications'

Meteor.startup(() => {
  // code to run on server at startup
});
